apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-app
  template:
    metadata:
      labels:
        app: go-app
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: go-app
        image: ghcr.io/jaimindevops/go-app:32558fb08c5a92b7e26a2d99addd1dd39e923ffa
        ports:
        - containerPort: 8080
        env:                     # Added your Redis Env
        - name: REDIS_URL
          value: "redis-service:6379"
        securityContext:         # Kept your Security Fixes
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: "100m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "128Mi"
