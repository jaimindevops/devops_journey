apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-app
  template:
    metadata:
      labels:
        app: go-app
    spec:
      containers:
      - name: go-app
        image: ghcr.io/jaimindevops/go-app:0d793f02f831cb5fa882b698b62d7c64e7f8a582
        resources:
          requests:
            cpu: "100m"     # 🚨 THIS FIXES THE HPA LOG ERROR
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        ports:
        - containerPort: 8080
